# Node compatability checker for Web3.js

This script requires node and npm to be installed.

Run the script as

```
npm start
```

As test node you can use ganache-cli (https://github.com/trufflesuite/ganache-cli)